# Modules

One folder per feature: `<feature>.schema.ts`, `<feature>.service.ts`, `<feature>.routes.ts`.
Register each in `index.ts` with a prefix. `health/` shows the smallest possible module.
Full template: [docs/architecture.md](../../../docs/architecture.md#adding-a-feature-module-template).
